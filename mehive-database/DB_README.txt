The db_legacy folder is no longer in use. Instead, there is an import scripts
that can be imported into MariaDB. Developers can make changes and re-export
the scripts. The legacy folder exists in case developers wish to run a local version.